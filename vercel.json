{
	"framework": null,
	"buildCommand": "npm run build",
	"outputDirectory": "build",
	"routes": [
		{ "handle": "filesystem" },
		{
			"src": "/_app/(.*)",
			"status": 404,
			"headers": { "Content-Type": "text/plain" }
		},
		{ "src": "/(.*)", "dest": "/index.html" }
	]
}
